import { createStore } from 'vuex'
import ModulePkmode from './pkMode'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    pkMode: ModulePkmode,
  }
})
